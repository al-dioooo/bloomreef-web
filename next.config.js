/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // basePath: "/profile",
    // trailingSlash: true,
    // assetPrefix: "/profile",
    reactStrictMode: true,
    env: {
        appName: "Bloomreef"
    }
}

module.exports = nextConfig
